---
interface Props {
    class?: string;
}

const { class: className } = Astro.props;
---

<div
    class={`rounded-lg border border-border bg-card text-card-foreground shadow-sm hover:border-border-strong hover:bg-surface-raised transition-colors ${
        className || ""
    }`}
>
    <slot />
</div>
